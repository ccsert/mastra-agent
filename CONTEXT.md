# 企业 Agent 能力平台

平台帮助业务团队快速接入与使用 AI 能力，包含集中控制面、运行能力和业务数据存储。用户可以直接在平台中使用，也可从业务系统调用或嵌入；运行与存储可由平台托管，也可部署在客户私有环境。

## Language

**客户**：使用平台 AI 能力的组织，可选择平台托管资源或使用自己的私有运行环境。首期默认以独立客户组织对应一个租户。
_Avoid_: 将客户、租户、项目直接视为同义词。

**租户**：客户资源与权限的隔离范围，下设多个项目。
_Avoid_: 因客户属于同一集团或使用相同托管设施就认为租户间资源自动共享。

**项目**：租户内组织 AI 能力、成员及协作授权的空间，可按团队或业务领域划分。
_Avoid_: 将项目与接入平台的某个业务应用强制一一对应。

**环境**：项目中的发布与使用目的地，例如测试或生产，具有对应的资源位置和授权范围。
_Avoid_: 将环境直接等同于一台服务器或一个 Runtime 进程。

**业务应用**：调用平台能力或承载嵌入式界面的第三方业务系统，也可以向平台注册工具或 MCP 服务。
_Avoid_: 与平台本身混称为应用。

**业务用户**：在业务应用中使用平台能力的人，其身份与业务应用自身的身份分别表达。
_Avoid_: 将同一业务应用下的所有使用者视为同一个用户。

**平台用户**：直接在平台界面使用或管理 AI 能力的人，可与业务应用中的使用者对应同一身份。
_Avoid_: 因入口不同就假定必须建立两套用户帐号，或默认平台用户拥有全部资源权限。

**应用身份**：业务应用在平台中接受识别并获得资源访问权限的主体身份。
_Avoid_: 将应用身份等同于某位业务用户，或将应用标识本身视为身份凭证。

**用户委托调用**：获准的业务应用代表被可信识别的业务用户调用平台能力，应用与用户身份共同进入权限判断和审计。
_Avoid_: 将请求中任意填写的用户标识视为可信委托。

**会话**：由用户或应用主体持有的一段持续交互历史，在项目环境与应用入口范围内管理，可按权限共享。
_Avoid_: 因共享 Agent 或匹配到同一用户就自动开放或合并全部聊天历史。

**控制面**：集中管理 Agent 能力及其发布和运行环境的管理侧。
_Avoid_: 将管理侧与实际执行侧合称为后端而省略边界。

**Runtime**：承载 Agent 与工作流执行能力的运行侧，可由平台托管或部署在客户私有环境中。
_Avoid_: 用控制面指代实际执行环境。

**平台托管 Runtime**：由平台运营和提供的 Runtime，供平台用户或业务应用使用。
_Avoid_: 将平台内使用仅理解为访问客户私有 Runtime 的控制台。

**客户私有 Runtime**：部署在客户指定私有环境中的 Runtime，按客户的访问与数据政策运行。
_Avoid_: 将业务系统嵌入界面直接等同于客户私有部署。

**客户连接器**：在客户环境主动连接平台，承接对已有业务工具或服务的获准调用的轻量组件。
_Avoid_: 将连接器等同于完整 Agent Runtime，或认为建立连接就授予了所有业务访问权限。

**资源位置**：一项能力实际运行或保存内容的环境，可由平台托管或由客户管理。
_Avoid_: 将资源在目录中的可见范围等同于它的执行与存储位置。

**默认位置**：项目环境为资源提供的默认运行或存储位置；知识库和工具可以明确指定其他位置。
_Avoid_: 将同一项目下的资源都视为必须存放和执行于同一环境。

**模型处理许可**：客户允许某个模型服务处理指定类别内容的权限，适用于客户自有或获准使用的外部模型。
_Avoid_: 将内容获准返回平台等同于它可交给任意模型服务处理。

**Agent**：可供平台内用户或业务应用使用、能够根据上下文自主选择已获授权工具的 AI 能力。可独立使用，也可参与工作流。
_Avoid_: 将 Agent 与固定工作流、模型、工具视为同义词，或认为调用 Agent 必须先创建工作流。

**Skill**：供 Agent 使用的可复用任务指导包，可携带说明、参考资源和脚本。
_Avoid_: 将 Skill 与一次工具调用或工作流视为同义词，或将包内声明等同于平台授予的权限。

**Skill 版本**：供 Agent 使用的一份固定 Skill 内容。内容修改产生新的版本，授权与使用记录明确指向相应版本。
_Avoid_: 将可修改的草稿与执行时绑定的版本混称。

**Skill 授权**：允许特定使用方在指定资源范围内执行某个 Skill 版本的许可。获准范围内的脚本调用可由 Agent 自主执行。
_Avoid_: 将上传、格式校验或包内工具声明等同于授予执行许可。

**工作流定义**：描述任务步骤、步骤关系和所需能力的可编辑流程。既可以由 AI 生成和修改，也可以通过可视化界面编辑。
_Avoid_: 将画布布局等同于流程的执行含义。

**工作流版本**：工作流发布后供执行使用的固定定义。修改草稿不会改变已绑定该版本的运行。
_Avoid_: 将正在编辑的草稿称为已发布流程。

**运行**：Agent 或工作流针对一次输入开展的具体执行，具有独立的状态和结果。
_Avoid_: 将工作流定义与一次运行混称为流程。

**AI 自动编排**：根据用户意图生成、修改完整工作流，并在发布后按版本执行。首期也允许 Agent 节点自主选择工具；运行中修改整体计划尚未纳入已确认范围。
_Avoid_: 将定义热更新等同于进行中的运行被修改。

**工具**：Agent 或工作流可以调用的具体业务操作，可以由平台提供或由业务应用注册。
_Avoid_: 与提供多项操作的 MCP 服务混称。

**工具能力版本**：某项工具的已记录接口与语义快照，供发布能力明确引用。
_Avoid_: 将平台保存的接口快照等同于远端业务实现也已被固定或能够回滚。

**动作批准**：对指定业务动作、参数和对象范围的执行许可，可针对一次调用，也可由客户明确授予自动执行范围。
_Avoid_: 将 Skill 脚本执行许可等同于批准其可能发起的所有业务写入。

**受管副本**：平台或客户运行环境能够登记、鉴权并执行生命周期操作的一份内容副本。
_Avoid_: 将撤销内容许可等同于已经删除全部副本，或声称平台能够擦除所有外部下载。

**知识库**：供 Agent 和业务应用使用的知识集合，其文档处理也可以通过平台工作流完成。
_Avoid_: 将知识库直接等同于某个存储组件。

**API SDK**：供业务应用以编程方式调用平台公开能力的客户端接口。
_Avoid_: 与包含可见界面的 UI SDK 混称。

**UI SDK**：业务应用可嵌入自身界面的平台交互组件集合。
_Avoid_: 与 API SDK 或整个管理控制台混称。

**iframe 嵌入**：业务应用通过嵌入平台页面来提供平台交互能力的接入形式。
_Avoid_: 将嵌入页面视为独立的 Agent 或运行体系。
